<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record id="as_template_crm_email_remplazo" model="mail.template">
            <field name="name">RELEVAMIENTO: Correo enviado al cliente y cotizador</field>
            <field name="model_id" ref="crm.model_crm_lead" />
            <field name="subject">Notificación de relevamiento: Ref(${object.name})</field>
            <field name="email_from">${object.company_id.partner_id.email}</field>
            <field name="email_to">${object.partner_id.email};${object.user_id.partner_id.email}</field>
            <field name="body_html" type="html">
                <div>
                    Estimado(s)<br />
                    Mediante la presente hacemos llegar el informe de revelamiento. Realizado en fecha
                    ${object.as_date_row} en (${object.street}).<br />
                    Cualquier consulta estamos a su disposición.<br />
                    <br /><br />
                    Saludos cordiales,


                    Se envia adjunta el informe del relevamiento (${object.name}) <br />
                    <br />
                    Gracias!!
                </div>
            </field>
        </record>

    </data>
</odoo>