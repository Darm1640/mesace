<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="as_viws_payslip_run" model="ir.ui.view">
        <field name="name">as.asignar.bonus.discount</field>
        <field name="model">as.asignar.bonus.discount</field>
        <field name="arch" type="xml">
            <form string="Compute Asset">
                <div>
                    <p>
                     Asiganr Bono o Descuento a Empleado en nomina
                    </p> 
                    <group>
                    <field name="as_playslip_run_id" />
                    </group>
                </div>
               
                <footer>
                    <button string="Procesar" name="actualizar_costos_productos" type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
         </field>
    </record>

    <record id="as_action_payslip_wizard" model="ir.actions.act_window">
        <field name="name">Asignacion Manual</field>
        <field name="res_model">as.asignar.bonus.discount</field>
        <field name="view_mode">tree,form</field>
        <field name="view_id" ref="as_viws_payslip_run"/>
        <field name="target">new</field>
        <field name="binding_model_id" ref="as_bo_hr_bonus_discount.model_as_employee_bonus" />
    </record>
   

</odoo>
