<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Payment sequences -->
        <record id="sequence_project_load" model="ir.sequence">
            <field name="name">Secuencia Solicitudes de materiales</field>
            <field name="code">as.project.code</field>
            <field name="prefix">SM/</field>
            <field eval="1" name="number_next"/>
            <field eval="1" name="number_increment"/>
            <field eval="True" name="use_date_range"/>
            <field eval="False" name="company_id"/>
            <field name="padding">4</field>
        </record>
       
    </data>
<data noupdate="1">
        <!-- Payment sequences -->
        <record id="sequence_nota_remision" model="ir.sequence">
            <field name="name">Secuencia Nota de Remisión</field>
            <field name="code">as.nota.remision.code</field>
            <field name="prefix"></field>
            <field eval="1" name="number_next"/>
            <field eval="1" name="number_increment"/>
            <field eval="True" name="use_date_range"/>
            <field eval="False" name="company_id"/>
            <field name="padding">4</field>
        </record>
       
    </data>
    <data noupdate="1">
        <!-- Payment sequences -->
        <record id="sequence_ot_prefactura" model="ir.sequence">
            <field name="name">Secuencia de ot factura</field>
            <field name="code">as.secuence.ot</field>
            <field name="prefix"></field>
            <field eval="1" name="number_next"/>
            <field eval="1" name="number_increment"/>
            <field eval="True" name="use_date_range"/>
            <field eval="False" name="company_id"/>
            <field name="padding">4</field>
        </record>
    </data>

        <!-- gastos sequences -->
        <record id="sequence_project_gastos" model="ir.sequence">
            <field name="name">Secuencia para gastos</field>
            <field name="code">as.gastos.code</field>
            <field name="prefix"></field>
            <field eval="1" name="number_next"/>
            <field eval="1" name="number_increment"/>
            <field eval="True" name="use_date_range"/>
            <field eval="False" name="company_id"/>
            <field name="padding">4</field>
        </record>
</odoo>
