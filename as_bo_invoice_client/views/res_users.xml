<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="as_res_users_form" model="ir.ui.view">
            <field name="name">as.res.users.form</field>
            <field name="model">res.users</field>
            <field name="inherit_id" ref="base.view_users_form" />
            <field name="arch" type="xml">
                <xpath expr="//group[@name='preferences']" position="before">
                    <group string="Facturación">
                        <field name="as_system_certificate" widget="many2many_tags" />
                        <field name="as_branch_office" widget="many2many_tags" />
                        <field name="as_pdv_ids" widget="many2many_tags" />
                    </group>
                </xpath>
            </field>
        </record>
    </data>
</odoo>